const officeList = require('../templates/office-list');
const refuge = require('../templates/refuge');
const hunt = require('../templates/hunt');
const huntUnits = require('../templates/huntUnits');
const specialHunts = require('../templates/specialHunts');

module.exports = {
  officeList,
  refuge,
  hunt,
  huntUnits,
  specialHunts,
};
